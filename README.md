# Binary Fields

## Functions to Implement

### Arithmetic Core
- `add(a, b)` 
- `sub(a, b)`
- `mul(a, b)`
- `square(a)`

### Field Structure
- `zero()`
- `one()`
- `is_zero(a)`

### Exponentiation
- `pow(a, n)`
- `invert(a)`

### Utilities
- `from_u64(val)`
- `to_u64(a)`